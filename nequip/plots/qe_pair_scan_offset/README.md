# Quantum ESPRESSO Pair Scan Offset Plots (`plots/qe_pair_scan_offset`)

offset = The right E0 has been used for the nequip models. This is true for all other models in this work (with the only exception of a few initial nequip trials)

This directory contains the 2-body potentials computed with different nequip models trained on dimer data obtained with the quantum espresso code.
