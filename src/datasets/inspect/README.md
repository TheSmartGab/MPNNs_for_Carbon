# Dataset Inspection (`datasets/inspect`)

This subdirectory contains utilities for inspecting and analyzing atomic simulation datasets in extxyz format. These tools generate histograms, correlation analyses, power spectra, and statistical summaries of dataset contents to validate data quality before training machine learning interatomic potentials.
