# Dataset Splitting (`datasets/split`)

This subdirectory contains utilities for splitting datasets into training, validation, and test sets for machine learning model development. These scripts handle systematic dataset partitioning based on filename parsing, structural configuration types, and subsampling strategies to ensure balanced representation across splits.
